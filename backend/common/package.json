{
  "name": "machsan",
  "version": "1.0.11",
  "description": "common",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "publish": "git add . && git commit -m \"Updated\" && npm version patch && npm publish --access=public"
  },
  "author": "Ron & Nir",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^4.0.1",
    "typescript": "^4.7.4"
  }
}
